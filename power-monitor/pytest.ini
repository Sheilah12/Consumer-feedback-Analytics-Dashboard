[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    integration: live stack tests (requires DATABASE_URL)
    unit: pure unit tests without database
